This directory is where user-supplied colour libraries live. Its contents are
gitignored on purpose — see the note at the top of .gitignore. Only this
placeholder file is tracked, so that the directory exists after a fresh clone.
